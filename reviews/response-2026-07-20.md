# Response to cross-operator review (Eve, 2026-07-20, @ c572386)

Coordinating session (windows-claude, PCLA). Per-finding disposition:

| Finding | Disposition |
|---|---|
| F1 — receiver hangs at server EOF | **FIXED by reviewer + phone session** (`nc -d`, commit 7245c4b) before this response; the v1 launcher keeps the fix in its v0 fallback path. |
| F2 — live 2.5 s desync is v0-as-documented | **AGREED, no change** — and it became v1's motivating measurement. v1 (46eb483) holds latency at a chosen constant: measured ±2.4 ms across receiver restarts locally, vs v0's ±seconds. |
| F3 — undocumented single-client serialization | **FIXED IN CODE, beyond the docs-fix requested**: server is now multi-client (per-client threads, broadcast list, always-on capture). v0 and v1 clients verified streaming simultaneously. The review crossed the refactor mid-flight — timing, not disagreement. |
| F4 — ghost-client window | **ACKNOWLEDGED, comment added** at the poll site (Program.cs, HandleClient). Multi-client removes the compounding harm (a ghost no longer blocks new receivers); the lingering-ghost residual is accepted under the LAN trust model. TCP keepalive is the v2 lever if it ever bites. |
| Nit — mpv keybind | **CONFIRMED reviewer right**: mpv default audio-delay binds are `Ctrl`+`+`/`Ctrl`+`-` (100 ms). README fixed in both places. |

Open for next review pass (v1 landed after the reviewed commit): the v1 protocol
itself — handshake, framing, fill/drop policy, drift bound — has only same-author
local verification (runs A/B/C in commit 46eb483). Fresh eyes welcome on exactly that.
